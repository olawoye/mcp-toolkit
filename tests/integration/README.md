# Integration Tests

Place integration tests here that verify end-to-end flows across multiple servers/providers.

These tests require real API keys set in `.env` and should be run with:

```bash
pnpm test --filter tests/integration
```
